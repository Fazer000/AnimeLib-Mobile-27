// Отладочная информация для WebView
console.log('[AnimeLIB] DOM ready state: ' + document.readyState);
console.log('[AnimeLIB] Document body exists: ' + (document.body ? 'yes' : 'no'));
console.log('[AnimeLIB] Current URL: ' + window.location.href);
console.log('[AnimeLIB] All buttons count: ' + document.querySelectorAll('button').length);
console.log('[AnimeLIB] All elements with btn class: ' + document.querySelectorAll('.btn').length);
'debug_info_ok';
