<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="?attr/rvItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:padding="8dp">

        <!-- Anime Cover -->
        <androidx.cardview.widget.CardView
            android:layout_width="60dp"
            android:layout_height="90dp"
            android:layout_gravity="center_vertical"
            app:cardCornerRadius="8dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="?attr/cardBackgroundColor">

            <ImageView
                android:id="@+id/searchItemCover"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="Anime Cover" />

        </androidx.cardview.widget.CardView>

        <!-- Anime Info -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <!-- Title -->
            <TextView
                android:id="@+id/searchItemTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="?attr/primaryTextColor"
                android:textSize="14sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end" />

            <!-- Chips (Type, Status, Year) -->
            <com.google.android.material.chip.ChipGroup
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                app:chipSpacingHorizontal="4dp"
                app:singleLine="true">

                <com.google.android.material.chip.Chip
                    android:id="@+id/searchItemTypeChip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="10sp"
                    android:textColor="?attr/primaryTextColor"
                    app:chipBackgroundColor="?attr/alphaBorderColor"
                    app:chipStrokeColor="?attr/alphaPrimaryColor"
                    app:chipStrokeWidth="0.5dp"
                    app:chipMinHeight="24dp"
                    app:chipCornerRadius="50dp"
                    app:ensureMinTouchTargetSize="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/searchItemStatusChip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="10sp"
                    android:textColor="?attr/primaryTextColor"
                    app:chipBackgroundColor="?attr/alphaBorderColor"
                    app:chipStrokeColor="?attr/alphaPrimaryColor"
                    app:chipStrokeWidth="0.5dp"
                    app:chipMinHeight="24dp"
                    app:chipCornerRadius="50dp"
                    app:ensureMinTouchTargetSize="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/searchItemYearChip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="10sp"
                    android:textColor="?attr/primaryTextColor"
                    app:chipBackgroundColor="?attr/alphaBorderColor"
                    app:chipStrokeColor="?attr/alphaPrimaryColor"
                    app:chipStrokeWidth="0.5dp"
                    app:chipMinHeight="24dp"
                    app:chipCornerRadius="50dp"
                    app:ensureMinTouchTargetSize="false"
                    android:visibility="gone" />

            </com.google.android.material.chip.ChipGroup>

            <!-- Rating -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:src="@drawable/ic_star"
                    app:tint="?attr/ratingStarColor"
                    android:layout_marginEnd="4dp"/>

                <TextView
                    android:id="@+id/searchItemRating"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="?attr/primaryTextColor"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/searchItemVotes"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:textColor="?attr/accentTextColor"
                    android:textSize="12sp" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>

